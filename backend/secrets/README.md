# Firebase service account

1. Firebase Console -> project **mediremind-79** -> Project settings -> Service accounts.
2. **Generate new private key** and download the JSON file.
3. Save the JSON file in this folder (any name is fine, e.g. `mediremind-79-firebase-adminsdk-....json`).
4. Run `docker compose restart backend` from the repo root.
